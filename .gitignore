# Gradle / Rust build output
.gradle/
build/
**/build/
target/
**/target/
!gradle/wrapper/gradle-wrapper.jar

# IDE / OS
.idea/
*.iml
.DS_Store
local.properties
.kotlin/
*.hprof

# Generated Xcode projects — the committed project.yml (XcodeGen) is the source of truth;
# run `xcodegen generate` to (re)produce these.
*.xcodeproj/

# Claude Code local state
.claude/

# Local code-walkthrough / onboarding notes (not part of the published docs)
walkthrough_docs/

# Local AI-chatbot context briefs (not part of the published docs)
ai-context/
