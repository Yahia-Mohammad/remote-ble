# 0.9.1 progress and handoff status

**Status:** implementation in progress — post-merge validation continuation point
**Updated:** 2026-07-17
**Branch:** `main` (tagged `v0.9.1`)

This is the continuation point for the 0.9.1 security, lifecycle, and conformance hardening work.
The accepted contract is in [0.9.1-hardening-decisions.md](0.9.1-hardening-decisions.md), and the
scenario catalogue is in [0.9.1-scenarios.md](../conformance/0.9.1-scenarios.md).

## Implemented

| Area | Outcome | Commit |
|---|---|---|
| Decisions and scenarios | Accepted D1–D5 contract and initial conformance catalogue | `3ecf360` |
| Transport cleanup | Rust writer failure retires its reader and joins work before grace cleanup | `2bfb633` |
| Logging | Atomic logger configuration and documented sink/re-entrancy contract | `dcb8e72` |
| Protocol negotiation | Kotlin/Rust version-range selection and fixed 1002 incompatibility close | `fcbe56d`, `4f415ef` |
| Safe bind | Desktop agents default to loopback; non-loopback requires credentials or explicit unsafe development override | `d3054fc` |
| Principals | Named credentials (`REMOTE_BLE_TOKENS`), legacy `REMOTE_BLE_TOKEN=default`, constant-time checks, and principal-scoped stable client IDs | `2d50300` |
| Lease semantics | Explicit Kotlin `Disconnect` releases immediately; only transport loss and unsolicited BLE drops use grace | `9c79181` |
| Frame limits | Both agents cap inbound WebSocket frames at 1 MiB before protocol decoding | `ec0f0c9` |
| Operation limits | Both agents cap scan filters at 64 and write/descriptor payloads at 512 bytes | `27e2169` |
| Deployment docs | Loopback, named credentials, principal resume, TLS/VPN guidance, and disconnect semantics documented | `a45d183` |
| Recovery | Failed reconnect prerequisites skip dependent subscription/connection-parameter replay; independent scans still replay | `5172078` |
| Review hardening | Restored authenticated mobile LAN serving; added framing-layer Rust limits, stable invalid-request errors, principal-safe logging, and regressions for limits, recovery, protocol close, and writer retirement | `a182f35` plus follow-up working-tree corrections |
| Review blockers | Kotlin derives terminal incompatibility from the actual WebSocket close reason; both agents reject duplicate live stable client identities with generation-bound release | `1b87725` |
| Management boundary | HTTP dashboard routes mount only with a distinct operator credential; reads reject anonymous and client credentials | `1b87725` |
| Resource invariants | 16 scans / 128 observations per connection; bounded SDK and Rust event queues; notification overflow ends the stream; fixed-memory failed-auth limits | `1b87725` |
| Mobile credential exposure | Masked phone credential field with no bearer rendering after start; documented Android DataStore/iOS `NSUserDefaults` launcher as dev/test-only storage | `1b87725` |
| Mobile runner lifecycle | One mutex-serialized lifecycle, observable `STOPPED`/`STARTING`/`RUNNING`/`STOPPING`/`FAILED` state, startup result, and structured best-effort stop result | `1b87725` |
| Peripheral shutdown | Additive `RemotePeripheral.shutdown(timeout)` requests remote release, guarantees local collector cleanup, and distinguishes completed/timed-out/transport-lost outcomes | `1b87725` |
| Client readiness | Additive `AgentSession.readiness` separates WebSocket connection, handshake, reconciliation, ready, degraded, terminal incompatibility, and closure; secret-free reconciliation report records replay outcome | `1b87725` |
| Second-review fixes | Kotlin `connectionLive` guard stops a connect that completes after transport retirement from resurrecting an abandoned lease (parity with Rust `connection_live`); operator/dashboard auth is rate-limited via a dedicated failed-auth limiter; both agents reject an out-of-range MTU as `INVALID_REQUEST`; duplicate-session transport-signal divergence documented as operational `LEASE-DUPLICATE-01` | `1b87725` |
| Mobile lifecycle host tests | Android JVM host-test target covers serialized concurrent starts/stops, cancelled start cleanup, safe startup failure/retry, and connected-peripheral teardown using the injected graph seam | `a0f3f21` |
| Operation-limit conformance | `LIMIT-OP-01` has paired Kotlin/Rust adapters for scan-filter, write/descriptor-value, and both MTU boundaries; each rejects before reaching the backend | `a0f3f21` |

## Review status

Two review passes are fully addressed. The first (`origin/main → a182f35` plus follow-up working-tree
corrections) cleared the mobile-bind regression, principal logging, Rust framing-layer limit,
limit-error classification, per-frame Kotlin double decode, radio-grace wording, and the protocol
terminal-state, Hello-validation, duplicate-session, management-read, and resource-invariant blockers
— all with regressions in both agents. The second focused review (2026-07-16) raised four items — the
late-connect lease guard, operator-auth rate limiting, MTU argument validation, and duplicate-session
documentation — now all resolved (final *Implemented* row).

## Verified at this continuation point

- `:protocol:jvmTest`, `:agent:jvmTest`, `:client-sdk:jvmTest`, and `:log:jvmTest` pass.
- `:agent:testAndroidHostTest` passes with the mobile runner lifecycle suite.
- `:android-agent:compileDebugKotlin` passes.
- Rust `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test --locked`
  pass (68 tests).
- Hardware evidence remains deferred as described below; this is implementation verification, not a
  release sign-off.

## Remaining 0.9.1 work

1. **Executable conformance:** `VERSION-01` and `LIMIT-OP-01` now have paired adapters. Grow that
   baseline to the remaining auth, lease, frame/stream, recovery, shutdown, and liveness scenarios,
   then add durable CI/release gates.

## Explicitly deferred

The 0.9.0/0.9.1 GitHub releases defer the following validation to the **0.10.0 hardware-validation
gate** before Maven Central publication:

- iOS runtime lifecycle evidence (the Android host suite already covers the shared runner core);
- the `TLS-PROXY-01` real `wss://` deployment path: upgrade, reconnect, notifications, bearer
  forwarding, and certificate trust; and
- archived CI/release evidence for the release commit and hardware run.

These are 0.10.0 release-gate requirements, not 0.9.1 completion criteria.

## Resume order

Grow executable conformance; run the deferred runtime, TLS, and release-evidence work as part of
the 0.10.0 hardware-validation gate.
